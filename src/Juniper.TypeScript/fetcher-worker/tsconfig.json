{
  "extends": "../../tsconfig.json",

  "include": [
    "src/**/*.ts"
  ],

  "exclude": [
    "node_modules"
  ],

  "compilerOptions": {
    "typeRoots": [
      "./node_modules/@types",
      "./node_modules/@juniper-types"
    ]
  }
}
