import { TimerTickEvent } from "juniper-tslib";
export declare class DecayingGain {
    private readonly input;
    private readonly output;
    min: number;
    max: number;
    threshold: number;
    attack: number;
    decay: number;
    sustain: number;
    hold: number;
    release: number;
    private readonly activity;
    private curLength;
    private timer;
    private _enabled;
    private shouldRun;
    constructor(audioCtx: AudioContext, input: AudioNode, output: GainNode, min: number, max: number, threshold: number, attack: number, decay: number, sustain: number, hold: number, release: number);
    update(evt: TimerTickEvent): void;
    get enabled(): boolean;
    setEnabled(v: boolean): void;
    start(): void;
    stop(): void;
    private refresh;
    get length(): number;
    private get time();
    private set time(value);
    private get attackStart();
    private get attackEnd();
    private get decayStart();
    private get decayEnd();
    private get holdStart();
    private get holdEnd();
    private get releaseStart();
    private get releaseEnd();
    private get attacking();
    private get decaying();
    private get holding();
    private get releasing();
    private get pAttack();
    private get pDecay();
    private get pRelease();
    private get value();
    get gain(): number;
}
